target/
harness/target

# work-in-progress, kept local only
examples/documentation.md
/scripts/

# Demo assets copied into docs/ at build time (canonical copies live in
# examples/data/; the wasm is built from source). `make demo` / CI regenerate.
docs/*.obj
docs/*.stl
docs/*.ply
docs/*.glb
docs/*.blg
docs/js/app.bundle.js
docs/js/package.json
idea.md
notes.md

# render-diff Action local output
render-diff-out/

# npm package: wasm staged from the build
packages/maquette-js/wasm/

# python build artifacts (maturin develop)
__pycache__/
crates/maquette-py/python/maquette/_maquette*.so
